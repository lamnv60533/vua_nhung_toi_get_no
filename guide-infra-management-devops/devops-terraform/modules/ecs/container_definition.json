[
  {
    "image": "${account_id}.dkr.ecr.${region}.amazonaws.com/${ecr_repo_name}:latest",
    "name": "${project}-${service}-ecs-container",
    "cpu": 512,
    "memoryReservation": 1024,
    "environment": [
      {
        "name": "REGION",
        "value": "${region}"
      },
      {
        "name": "DYNAMO_TABLE",
        "value": "${dynamo_table}"
      },
      {
        "name": "S3_BUCKET",
        "value": "${s3_bucket}"
      },
      {
        "name": "TARGET_ACCOUNT_ID",
        "value": "${target_account_id}"
      },
      {
        "name": "TARGET_ROLE_NAME",
        "value": "${target_role_name}"
      }
    ],
    "command": [],
    "dnsSearchDomains": [],
    "dnsServers": [],
    "dockerSecurityOptions": [],
    "entryPoint": [],
    "links": [],
    "systemControls": [],
    "ulimits": [
      {
        "name": "nofile",
        "softLimit": 65535,
        "hardLimit": 65535
      }
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "secretOptions": [],
      "options": {
        "awslogs-group": "/ecs/logs/${project}-${service}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "${service}"
      }
    },
    "mountPoints": [],
    "volumesFrom": [],
    "essential": true,
    "portMappings": [
      {
        "containerPort": 8080,
        "hostPort": 8080,
        "protocol": "tcp"
      }
    ],
    "healthCheck": {
      "command": [
        "CMD-SHELL",
        "curl -f http://localhost:8080/api/v1/health || exit 1"
      ],
      "interval": 5,
      "retries": 3,
      "timeout": 5
    }
  }
]